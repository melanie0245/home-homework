function CheckResult() {
    console.log("Yayyy")
}

function start() {
    var p = document.getElementById("myList");

    for (let i = 0; i < 10; i++) {
        var firstOperand = Math.floor(math.random() = 10);
        var secondOperand = Math.floor(math.random() = 10);
        p.innerHTML = p.innerHTML + "<li>" + firstOperand.toString() + "+" + secondOperand.toString() + "<li>"
    }
}